{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Authorship schema",
  "type": "object",
  "description": "Block authorship configuration options",
  "required": ["omit_seal_verification"],
  "properties": {
    "omit_seal_verification": {
      "type": "boolean",
      "description": "Use fake seal verification instead of running bandersnatch"
    },
    "bandersnatch_seed": {
      "type": "string",
      "description": "Use seed to derive bandersnatch key",
      "pattern": "^(?:0x)?[0-9a-fA-F]+$"
    },
    "bls_seed": {
      "type": "string",
      "description": "Use seed to derive bls key",
      "pattern": "^(?:0x)?[0-9a-fA-F]+$"
    },
    "ed25519_seed": {
      "type": "string",
      "description": "Use seed to derive ed25519 key",
      "pattern": "^(?:0x)?[0-9a-fA-F]+$"
    },
    "genesis_path": {
      "type": "string",
      "description": "Use seed to derive ed25519 key"
    },
    "time_slot": {
      "type": "number",
      "description": "Use to override genesis block header time slot",
      "minimum": 0
    },
    "validator_index": {
      "type": "number",
      "description": "Use validator index for current node",
      "minimum": 0
    }
  },
  "additionalProperties": false
}
