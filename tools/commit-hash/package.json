{
  "name": "@typeberry/commit-hash-to-blockchain",
  "version": "0.0.1",
  "description": "Commit git hashes to a public blockchain.",
  "main": "index.ts",
  "devDependencies": {
    "@octokit/webhooks-types": "^7.5.1",
    "@polkadot/api": "^12.2.1",
    "@polkadot/types": "^12.2.1",
    "@typeberry/logger": "0.0.1"
  },
  "scripts": {
    "start": "tsx ./index.ts",
    "test": "node --test --import tsx $(find . -type f -name '*.test.ts' | tr '\\n' ' ')"
  },
  "author": "Fluffy Labs",
  "license": "MPL-2.0",
  "type": "module"
}
