import { RegisterIndexDecoder } from "./register-index-decoder";

import assert from "node:assert";
import { test } from "node:test";

test("RegisterIndexDecoder", async (t) => {
	await t.test("Single register ", () => {});
});
